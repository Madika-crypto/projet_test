{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ category.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="mt-3" href="{{ path('category_index') }}">retourner à la liste</a></br>

    <a class="mt-3 mb-3 btn btn-warning" href="{{ path('category_edit', {'id': category.id}) }}">détails</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
